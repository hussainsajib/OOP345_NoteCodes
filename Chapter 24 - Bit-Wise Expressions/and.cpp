// Bit-wise and
// and.cpp
#include <iostream>
int main(){
    unsigned char u = 27u;
    unsigned char v = 14u;
    std::cout << (unsigned)u << " & " << (unsigned)v << " = " << (u & v) << std::endl;
}